extends layout

include utils.pug

block content
  form(action='/users/register' method='post')
    input(type='hidden' name='_csrf' value=csrfToken)
      +field('Dinosaur', userName, username.userName)
      +field('Email Address', 'emailAddress', username.emailAddress)
      +field('Password', 'password', username.password, 'password')
      +field('Confirm Password', 'confirmPassword', '', 'password')
      div
        button(type='submit') Register
        a(href='/') Cancel
  div
    p: a(href='/users/login') Already a dinosaur?
